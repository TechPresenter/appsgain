Drop design reference images here (PNG/JPG, any size).
Claude downscales them automatically before reading, which avoids the
chat attachment size limit that rejects large pastes.

Example: refs/footer-reference.png
